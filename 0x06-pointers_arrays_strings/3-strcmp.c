#include"main.h"

/**
 * _strcmp -  function that compares two strings.
 * @s1: a string
 * @s2: a string
 * Return: intiger
*/
int _strcmp(char *s1, char *s2)
{
int n = 0;
int result;
result = s1[n] - s2[n];
return (result);
}
