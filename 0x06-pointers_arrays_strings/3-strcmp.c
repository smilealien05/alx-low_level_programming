#include"main.h"

/**
 * _strcmp -  function that compares two strings.
 * @s1: a string
 * @s2: a string
 * Return: intiger
*/
int _strcmp(char *s1, char *s2)
{
int result;

result = s1[0] - s2[0];
return (result);
}
